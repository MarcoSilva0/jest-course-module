describe('Testado a persistencia de dados', () => {
  it('Test Description (IT)', () => {
    const number = 1;
    expect(number).toBe(1);
  });
  test('Test Description (TEST)', () => {
    const nome = 'Marco';
    expect(nome).toBe('Marco');
  });
});
